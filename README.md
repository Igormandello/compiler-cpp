# Compiler - C++
- ### Lexical Analyzer
  The Lexer is responsible for doing the spliting of the program in tokens, and return they one by one on nextSlice method
- ### Syntax Analyzer
  The Parser is responsible for checking the syntax of the program, analyzing if the types of parameters and assignments are correct and if variables are declared, throwing exceptions if something is wrong